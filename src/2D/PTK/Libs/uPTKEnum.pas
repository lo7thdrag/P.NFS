unit uPTKEnum;

interface

type
  TLocalCtrlState = (lcCombat, lcCheck);
  TOperationMode = (omWait, omInd, omAutonomous, omDAttack, omVFire);
  TDrumSelection = (dsLDrum, dsRDrum);
  TRateOfFire = (frHigh, frMiddle, frLow);

implementation

end.
